class Solution {
    public String sortVowels(String s) {
        List<Character>vowels = new ArrayList<>();
        for(char c : s.toCharArray()){
            if("AEIOUaeiou".indexOf(c) != -1){
                vowels.add(c);
            }

        }
        Collections.sort(vowels);

        StringBuilder res = new StringBuilder();
        int vIndex = 0;
        for(char c: s.toCharArray()){
            if("AEIOUaeiou".indexOf(c) !=-1){
                 res.append(vowels.get(vIndex++));

            }
            else{
                res.append(c);
            }
        }

        return res.toString();
        
    }
}


if they not ASCII then we can sort by timsort(selsort+inssort) but here we do with custom comparator with tim, 

" vow.sort((a,b)-> Character.compare( Charcter.toLowerCase(a) , Charcter.toLowerCase(b))); "

// Sort Vowels in a String
